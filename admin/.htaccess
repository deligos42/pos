# Protect admin directory - keep directory listings off.
# Session-based authentication is enforced by the PHP application.
Options -Indexes

# Deny direct access to sensitive files (Apache 2.4+ syntax)
<FilesMatch "\.(log|sql|db)$">
    Require all denied
</FilesMatch>
