# Local MySQL settings
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=pos_system

# Local SMTP settings for password resets
# MAIL_SMTP_HOST=smtp.example.com
# MAIL_SMTP_PORT=587
# MAIL_SMTP_ENCRYPTION=tls
# MAIL_SMTP_USERNAME=your_smtp_username
# MAIL_SMTP_PASSWORD=your_smtp_password
# MAIL_FROM_ADDRESS=no-reply@example.com
# MAIL_FROM_NAME="POS System"

# Railway or production MySQL URL support (optional)
# DATABASE_URL=mysql://root:password@host:port/dbname
# MYSQL_URL=mysql://root:password@host:port/dbname
